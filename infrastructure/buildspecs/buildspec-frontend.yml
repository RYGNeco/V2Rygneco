version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install -g pnpm
      - cd frontend
      - pnpm install
  build:
    commands:
      - pnpm run build
artifacts:
  files:
    - '**/*'
  base-directory: frontend/dist
